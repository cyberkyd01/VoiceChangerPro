<Application x:Class="VoiceChanger.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:conv="clr-namespace:VoiceChanger.App.Converters"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Dark" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>

            <conv:BoolToVisibilityConverter x:Key="BoolToVis" />
            <conv:BoolToVisibilityConverter x:Key="InverseBoolToVis" Invert="True" />
            <conv:NullToVisibilityConverter x:Key="NullToCollapsed" />
            <conv:NullToVisibilityConverter x:Key="NotNullToCollapsed" Invert="True" />
            <conv:InverseBoolConverter x:Key="InverseBool" />
            <conv:NotNullToBoolConverter x:Key="NotNullToBool" />
            <conv:DbConverter x:Key="Db" />
            <conv:StateToBrushConverter x:Key="StateBrush" />

            <SolidColorBrush x:Key="AccentBrush" Color="#5865F2" />
            <SolidColorBrush x:Key="SuccessBrush" Color="#3DD68C" />
            <SolidColorBrush x:Key="WarningBrush" Color="#F5A623" />
            <SolidColorBrush x:Key="DangerBrush" Color="#F04747" />
            <SolidColorBrush x:Key="MutedBrush" Color="#8A8F98" />
            <SolidColorBrush x:Key="CardBrush" Color="#1E2229" />
            <SolidColorBrush x:Key="CardBorderBrush" Color="#2C313A" />
            <SolidColorBrush x:Key="PanelBrush" Color="#171A20" />

            <Style x:Key="SectionTitle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="15" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                <Setter Property="Margin" Value="0,0,0,8" />
            </Style>
            <Style x:Key="Muted" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>
            <Style x:Key="Panel" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource CardBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="Padding" Value="14" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
